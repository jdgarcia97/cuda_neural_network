# TODO

1. Add mini-batch training for better GPU utilization.
2. Add model save/load so the network does not retrain every run.
3. Add shuffling each epoch.
4. Add a confusion matrix to see which digits the model misses.
5. Replace logistic + MSE with softmax + cross-entropy.
